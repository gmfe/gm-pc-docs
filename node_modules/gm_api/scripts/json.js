const { doProto, shellExec, getProtoPath } = require('./util')

function doJson({ protoName }) {
  const p = getProtoPath(protoName)
  shellExec(`npx pbjs -t json -p ./src/ --keep-case ${p} > ${p}.json`)
}

doProto('json', doJson)
