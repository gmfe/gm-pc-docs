import React, { FC } from 'react'
import { Select, SelectProps } from '@gm-pc/react'
import {
  SelectList,
  SelectEnum,
  MoreSelectList,
  MapId,
} from '../util/pc_util'
import type {
  MapProps,
  SelectListProps,
  SelectEnumProps,
  MoreSelectListProps,
  MapIdProps,
} from '../util/pc_util'
import {
  list_PresetType,
  map_PresetType,
} from './index'

export const Select_PresetType: FC<SelectEnumProps<number>> = ({ ...rest }) => {
  return <SelectEnum {...rest} data={list_PresetType} />
}

export const Map_PresetType: FC<MapProps> = ({ value }) => {
  return <>{map_PresetType[value]}</>
}

export type {
  MapProps,
  SelectListProps,
  MoreSelectListProps,
}
