import React, { FC } from 'react'
import { Select, SelectProps } from '@gm-pc/react'
import {
  SelectList,
  SelectEnum,
  MoreSelectList,
  MapId,
} from '../util/pc_util'
import type {
  MapProps,
  SelectListProps,
  SelectEnumProps,
  MoreSelectListProps,
  MapIdProps,
} from '../util/pc_util'
import {
  list_PinyinRequest_Style,
  map_PinyinRequest_Style,
} from './index'

export const Select_PinyinRequest_Style: FC<SelectEnumProps<number>> = ({ ...rest }) => {
  return <SelectEnum {...rest} data={list_PinyinRequest_Style} />
}

export const Map_PinyinRequest_Style: FC<MapProps> = ({ value }) => {
  return <>{map_PinyinRequest_Style[value]}</>
}

export type {
  MapProps,
  SelectListProps,
  MoreSelectListProps,
}
