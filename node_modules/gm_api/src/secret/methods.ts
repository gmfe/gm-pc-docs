import { Request } from '@gm-common/x-request'
import {
  GetQiniuUploadTokenRequest, GetQiniuUploadTokenResponse,
} from './types'
export const GetQiniuUploadToken = (r?: GetQiniuUploadTokenRequest, codes?: number[]) =>
  Request<GetQiniuUploadTokenResponse>('/ceres/secret/SecretService/GetQiniuUploadToken')
    .code([...(codes || [])])
    .data(r || {})
    .run()
GetQiniuUploadToken.url = '/ceres/secret/SecretService/GetQiniuUploadToken'