import React, { FC } from 'react'
import { Select, SelectProps } from '@gm-pc/react'
import {
  MapProps,
  SelectListProps,
  SelectList,
  MoreSelectListProps,
  MoreSelectList,
  MapIdProps,
  MapId,
} from '../pc_util'
import {
  list_FileType,
  map_FileType,
} from './index'

export const Select_FileType: FC<Omit<SelectProps<number>, 'data'>> = ({ ...rest }) => {
  return <Select {...rest} data={list_FileType} />
}

export const Map_FileType: FC<MapProps> = ({ value }) => {
  return <>{map_FileType[value]}</>
}

export type {
  MapProps,
  SelectListProps,
  MoreSelectListProps,
}
