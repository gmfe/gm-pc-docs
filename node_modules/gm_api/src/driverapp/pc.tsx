import React, { FC } from 'react'
import { Select, SelectProps } from '@gm-pc/react'
import {
  SelectList,
  SelectEnum,
  MoreSelectList,
  MapId,
} from '../util/pc_util'
import type {
  MapProps,
  SelectListProps,
  SelectEnumProps,
  MoreSelectListProps,
  MapIdProps,
} from '../util/pc_util'
import {
  list_ListTaskRequest_TaskStatus,
  map_ListTaskRequest_TaskStatus,
} from './index'

export const Select_ListTaskRequest_TaskStatus: FC<SelectEnumProps<number>> = ({ ...rest }) => {
  return <SelectEnum {...rest} data={list_ListTaskRequest_TaskStatus} />
}

export const Map_ListTaskRequest_TaskStatus: FC<MapProps> = ({ value }) => {
  return <>{map_ListTaskRequest_TaskStatus[value]}</>
}

export type {
  MapProps,
  SelectListProps,
  MoreSelectListProps,
}
