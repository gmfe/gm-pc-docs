import React, { FC } from 'react'
import { Select, SelectProps } from '@gm-pc/react'
import {
  SelectList,
  SelectEnum,
  MoreSelectList,
  MapId,
} from '../util/pc_util'
import type {
  MapProps,
  SelectListProps,
  SelectEnumProps,
  MoreSelectListProps,
  MapIdProps,
} from '../util/pc_util'
import {
  Route,
  ListRoute,
  ListRouteResponse,
} from './index'

export const Select_Route: FC<SelectListProps<
  Route,
  ListRouteResponse
>> = (props) => {
  return (
    <SelectList<Route, ListRouteResponse>
      {...props}
      messageName='Route'
      fetchList={ListRoute}
    />
  )
}

export const MoreSelect_Route: FC<MoreSelectListProps<
  Route,
  ListRouteResponse
>> = (props) => {
  return (
    <MoreSelectList<Route, ListRouteResponse>
      {...props}
      messageName='Route'
      fetchList={ListRoute}
    />
  )
}

export type {
  MapProps,
  SelectListProps,
  MoreSelectListProps,
}
